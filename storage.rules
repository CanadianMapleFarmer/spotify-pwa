rules_version = '2';

// No client-facing Storage usage remains — the Scene media pipeline was
// decommissioned (Scene is rendered procedurally on the client). Deny all.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
